#include <stdio.h>

int main()
{
    long long a, b, c = 0;
    long long sum = 0;   
        scanf("%lld %lld %lld",&a ,&b, &c);
    sum = a + b + c;
    printf("%lld\n", sum);
    
    return 0;
}