#include <stdio.h>
int main()

    {
        int x,y,n,m = 0;

        scanf("%d %d", &x, &y);
        scanf("%d",&n);
        scanf("%d",&m);

        printf("%d", n*m);

        return 0;
    }